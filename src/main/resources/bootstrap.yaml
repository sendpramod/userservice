spring:
  application:
    name: userservice
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8585/config-server
      name: ${spring.application.name}
      fail-fast: true